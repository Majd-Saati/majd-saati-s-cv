/** Site-level settings that are not part of the CV itself. */
export const site = {
  /**
   * Social share image (1200×630 PNG/JPG) under `/public`, e.g. `/og-image.png`.
   * Open Graph image tags are only emitted when this is set.
   */
  ogImage: undefined as string | undefined,
  themeColor: { light: '#fafafa', dark: '#09090b' },
};
